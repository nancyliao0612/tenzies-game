function Die({ value }) {
  return (
    <section className="die-wrapper">
      <div>{value}</div>
      <div>{value}</div>
      <div>{value}</div>
      <div>{value}</div>
      <div>{value}</div>
      <div>{value}</div>
      <div>{value}</div>
      <div>{value}</div>
      <div>{value}</div>
      <div>{value}</div>
    </section>
  );
}

export default Die;
